# Cursor Pilot — T0-1

## HANDOFF_TO_CURSOR_PILOT

**CURSOR_PILOT_PROMPT**

Create `projects/abc-snake/` using Vite `react-ts` template. Add Tailwind (`tailwindcss`, `postcss`, `autoprefixer` or Tailwind v4 per Vite docs). Include `index.html`, `vite.config.ts`, `tsconfig.json` / `tsconfig.app.json`, `src/main.tsx`, `src/App.tsx`, `src/index.css` with `@tailwind` directives. Add minimal `README.md` with `npm install`, `npm run dev`, `npm run build`. Ensure path alias `@/` optional. No game code yet—placeholder App is fine.

**STOP:** After scaffold builds with `npm run build`.
