# Scoper — T0-1

**handoff_id:** `abc-snake-v1`  
**task_id:** `T0-1`  
**scope_summary:** Scaffold `projects/abc-snake/` with Vite + React + TypeScript + Tailwind (index.html, package.json, vite.config.ts, tsconfig, entry files).

## Acceptance criteria
1. `projects/abc-snake/` exists with runnable `npm install` + `npm run dev`.
2. React 18+ and TypeScript strict enough for the game.
3. Tailwind CSS v4 or v3 wired for `src/` styles.
4. README with dev commands.

## Out of scope
- Game logic (later tasks).
