HANDOFF_TO_CURSOR_PILOT
  scope_summary: "Mirror the canon-systems-v2 Terraform root into canon-systems/infra/terraform/ so the live AWS resources currently backing KNOWLEDGE_API_URL and KNOWLEDGE_WORKER_URL (plus the supporting VPC/RDS/S3/secrets/baseline-ECS/ECR plane in account 222274634742 us-east-1) become declarative IaC owned by this repo. This is an IMPORT task, not a provision task: the files must parse locally (`terraform init -backend=false && terraform validate` exit 0) and a per-resource import manifest must enumerate every expected `terraform import <addr> <real_id>` command, but this session has no AWS credentials and MUST NOT run any state-mutating cloud command. The backlog AC 'terraform plan shows no changes against production' is therefore CARRIED to a follow-up human-in-the-loop step (flagged OQ-E0-T4-01) and not accepted as a done_signal for this task. MEMORY_ADAPTER_URL has no dedicated AWS resource per the Wave 0 audit (no ECR, no ECS service; localhost-only in dev) — E0-T4 MUST NOT invent IaC for a non-existent runtime; its absence is captured in the README + OQ-E0-T4-02. The v2 tfvars ECR list of four repos (jira-bridge, knowledge-api, knowledge-worker, temporal-runtime) is preserved verbatim so imports match live resources; deletion of the two non-URL repos (jira-bridge, temporal-runtime) is out of scope and would cause drift. Project_name stays 'canon-systems-v2' and environment stays 'dev' for parity with production state; renaming is flagged OQ-E0-T4-03 for a later task. Files copied: the six modules (vpc, ecr, ecs-fargate, rds-postgres, s3-artifacts, secrets) plus root `main.tf`, `variables.tf`, `outputs.tf`, `providers.tf`, `versions.tf`, `terraform.tfvars`. Files excluded verbatim: `terraform.tfstate*` (contains secrets), `.terraform/` (provider cache), `.terraform.lock.hcl` (re-generated by init). Existing `canon-systems/infra/auth-ingress/` is untouched. Living-spec updated (README, CHANGELOG, docs/SYSTEM-WORKFLOW.md §10 augment, docs/DEPRECATIONS.md note on v2 infra/). A new `tests/test_infra_layout.py` asserts file presence + the absence of the tfstate exclusion; root pytest stays green. Branch stays `wave/0/canon-memory-v1`; no commit / push performed inside this task."

  scope_packet:
    identifiers:
      handoff_id: "canon-memory-v1"
      plan_id: "canon_memory_platform_build_d21073e1"
      task_id: "E0-T4"
      workstream_id: "wave-0d"
      epic_id: "E0"
      agent_name: "scoper"
      repository_id: "canon-systems @ wave/0/canon-memory-v1 (tip 35df118)"
      upstream_source_repo: "canon-systems-v2 @ /Users/edwardwalker/localwork/canon-systems-v2 (sibling, read-only)"
      upstream_source_commit: "ebecb91"
      aws_account: "222274634742"
      aws_region: "us-east-1"
      live_project_name: "canon-systems-v2"
      live_environment: "dev"

    story:
      title: "Stand up infra/ by importing existing AWS resources"
      acceptanceCriteria:
        - "infra/terraform/ exists and is a byte-faithful copy of canon-systems-v2/infra/terraform/ MINUS terraform.tfstate, terraform.tfstate.backup, .terraform/, and .terraform.lock.hcl."
        - "The six modules (vpc, ecr, ecs-fargate, rds-postgres, s3-artifacts, secrets) and six root files (main.tf, variables.tf, outputs.tf, providers.tf, versions.tf, terraform.tfvars) are all present and unchanged from v2 @ ebecb91."
        - "`terraform init -backend=false && terraform validate` exits 0."
        - "infra/terraform/README.md enumerates, per resource address, the real AWS identifier and the exact `terraform import <addr> <real_id>` command."
        - "docs/E0-T4-INFRA-IMPORT.md records v2 SHA ebecb91, exclusion list, project_name+environment preservation rationale, and the explicit statement that E0-T4 ran no cloud commands."
        - "tests/test_infra_layout.py asserts presence of every expected file under infra/terraform/**, asserts absence of tfstate/lock/cache, and asserts infra/auth-ingress/ is unchanged."
        - "Root pytest collection stays green."
        - "docs/SYSTEM-WORKFLOW.md §10 augment + docs/DEPRECATIONS.md + README + CHANGELOG updated."
        - "No writes to backend/**, canon-systems-v2/**, infra/auth-ingress/**, src/canon_systems/**, Dockerfiles, deploy/**, or .github/workflows/**."
      done_signal:
        - "`cd infra/terraform && terraform init -backend=false` succeeds"
        - "`cd infra/terraform && terraform validate` exits 0"
        - "`pytest tests/test_infra_layout.py -q` exits 0"
        - "Root `pytest -q` exits 0"
        - "docs/E0-T4-INFRA-IMPORT.md cites v2 SHA ebecb91"
      deferred_done_signal_from_backlog:
        - criterion: "terraform plan shows no changes against production"
          deferred_to: "OQ-E0-T4-01 (operator-run reconciliation; post-merge)"

    repository:
      primaryLanguages: ["HCL (Terraform)", "Markdown", "Python 3.10+ (tests)"]
      terraform_version_required: ">=1.5.0"
      provider_pins: {aws: "~> 5.0", random: "~> 3.6"}
      reference_files_in_sibling_repo_readonly:
        - "/Users/edwardwalker/localwork/canon-systems-v2/infra/terraform/{main.tf,variables.tf,outputs.tf,providers.tf,versions.tf,terraform.tfvars}"
        - "/Users/edwardwalker/localwork/canon-systems-v2/infra/terraform/modules/{vpc,ecr,ecs-fargate,rds-postgres,s3-artifacts,secrets}/{main.tf,variables.tf,outputs.tf}"
        - "/Users/edwardwalker/localwork/canon-systems-v2/infra/terraform/terraform.tfstate (READ-ONLY source of real IDs; MUST NOT be copied)"
      git_history_status:
        preservation_decision: "explicit_waiver (same rationale as E0-T3; single-commit v2 history)"

    in_scope_paths_to_create:
      - "infra/terraform/main.tf"
      - "infra/terraform/variables.tf"
      - "infra/terraform/outputs.tf"
      - "infra/terraform/providers.tf"
      - "infra/terraform/versions.tf"
      - "infra/terraform/terraform.tfvars"
      - "infra/terraform/modules/vpc/{main.tf,variables.tf,outputs.tf}"
      - "infra/terraform/modules/ecr/{main.tf,variables.tf,outputs.tf}"
      - "infra/terraform/modules/ecs-fargate/{main.tf,variables.tf,outputs.tf}"
      - "infra/terraform/modules/rds-postgres/{main.tf,variables.tf,outputs.tf}"
      - "infra/terraform/modules/s3-artifacts/{main.tf,variables.tf,outputs.tf}"
      - "infra/terraform/modules/secrets/{main.tf,variables.tf,outputs.tf}"
      - "infra/terraform/README.md"
      - "infra/README.md"
      - "docs/E0-T4-INFRA-IMPORT.md"
      - "tests/test_infra_layout.py"

    in_scope_paths_to_modify:
      - "README.md"
      - "CHANGELOG.md"
      - "docs/SYSTEM-WORKFLOW.md"
      - "docs/DEPRECATIONS.md"
      - ".gitignore"

    out_of_scope_paths:
      - "backend/**"
      - "src/canon_systems/**"
      - "infra/auth-ingress/**"
      - "canon-systems-v2/**"
      - "pyproject.toml (root), pytest.ini"
      - "any Dockerfile, deploy/**, .github/workflows/**"

    forbidden_surface:
      no_cloud_commands:
        - "terraform apply, destroy, import, refresh"
        - "aws *, aws-vault *, AWS SDK invocation"
      permitted_cloud_commands:
        - "terraform init -backend=false"
        - "terraform validate"
        - "terraform fmt -check"

    acceptable_scope_expansion:
      pre_authorized:
        - "Within-major AWS provider bump (~> 5.0 → ~> 5.80) if init/validate fails"
        - "Byte-identical upstream fixes for deprecation warnings"
        - "Terraform artifact .gitignore entries under infra/terraform/"
        - "Reading v2 terraform.tfstate for real IDs (NOT copying)"
      not_pre_authorized:
        - "Adding modules not in v2 (e.g., DynamoDB is E2-T1)"
        - "Renaming project_name or environment"
        - "Pruning ECR repos from tfvars list"
        - "Touching infra/auth-ingress/**"

    openQuestions:
      - id: "OQ-E0-T4-01"
        question: "Zero-drift AC ('terraform plan shows no changes') cannot be verified without AWS creds."
        proposed_resolution: "Defer to operator follow-up post-wave-0 merge; E0-T4 done_signal is 'files parse + manifest ready'."
        blocking_for_this_task: false
      - id: "OQ-E0-T4-02"
        question: "Does MEMORY_ADAPTER_URL get a runtime AWS resource in Wave 0?"
        proposed_resolution: "Carried. E1-T2 owns the decision."
        blocking_for_this_task: false
      - id: "OQ-E0-T4-03"
        question: "Rename project_name from 'canon-systems-v2' to 'canon-systems'?"
        proposed_resolution: "NO (would cause delete/recreate). Defer to follow-up state-mv playbook."
        blocking_for_this_task: false
      - id: "OQ-E0-T4-04"
        question: "Prune jira-bridge, temporal-runtime from ECR list?"
        proposed_resolution: "NO (would destroy live resources). Keep verbatim; Wave 7 may revisit."
        blocking_for_this_task: false
      - id: "OQ-E0-T4-05"
        question: "Declare S3+DynamoDB remote backend now?"
        proposed_resolution: "Defer to Wave 2 (E2-T1 introduces DynamoDB)."
        blocking_for_this_task: false
      - id: "OQ-E0-T4-06"
        question: "Copy .terraform.lock.hcl?"
        proposed_resolution: "NO (machine-dependent). Operator regenerates."
        blocking_for_this_task: false

    prior_work_references:
      - "docs/WAVE-0-AUDIT.md (E0-T1)"
      - ".cursor/handoffs/canon-memory-v1/E0-T1/scoper.md"
      - ".cursor/handoffs/canon-memory-v1/E0-T2/scoper.md"
      - ".cursor/handoffs/canon-memory-v1/E0-T3/scoper.md"
      - "docs/E0-T3-MIGRATION-NOTES.md"
      - "/Users/edwardwalker/localwork/canon-systems-v2/infra/terraform/** @ ebecb91"
      - "docs/DEPRECATIONS.md"

    dor_checklist:
      all: "pass"

END_HANDOFF_TO_CURSOR_PILOT
